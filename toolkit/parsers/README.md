# Output Parsers

This directory contains utilities for parsing STDOUT/STDERR from legacy binaries.

## Planned Parsers

- `stdout-parser.js` - Generic STDOUT parsing with regex patterns
- `stderr-parser.js` - Error message extraction and formatting
- `result-extractor.js` - Universal result extraction utilities

These parsers handle the conversion from legacy text output to modern JSON responses.
